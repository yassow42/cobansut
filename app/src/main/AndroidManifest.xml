<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.creativeoffice.cobansut">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".cerkez.MapsActivityCerkez"
            android:label="@string/title_activity_maps_cerkez"></activity>
        <activity android:name=".cerkez.TeslimEdilenlerActivityCerkez" />
        <activity android:name=".cerkez.MusterilerActivityCerkez" />
        <activity android:name=".cerkez.SiparisActivityCerkez" />
        <activity android:name=".cerkez.SiparisActivity" />
        <activity
            android:name=".Activity.AdresBulmaMapsActivity"
            android:label="@string/title_activity_adres_bulma_maps" />
        <activity
            android:name=".CorluActivity.AdresBulmaMapsCorluActivity"
            android:label="@string/title_activity_adres_bulma_maps_corlu" />
        <activity
            android:name=".CorluActivity.MapsCorluActivity"
            android:label="@string/title_activity_maps_corlu" />
        <activity android:name=".CorluActivity.MusterilerCorluActivity" />
        <activity android:name=".CorluActivity.TeslimCorluActivity" />
        <activity android:name=".genel.BolgeSecimActivity" />
        <activity android:name=".CorluActivity.SiparislerCorluActivity" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Activity.GidilenMusteriActivity" />
        <activity android:name=".MusterilerActivity" />
        <activity android:name=".Activity.TeslimActivity" />
        <activity android:name=".Activity.SiparislerActivity" />
        <activity android:name=".genel.LoginActivity" />
        <activity android:name=".genel.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>